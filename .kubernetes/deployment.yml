apiVersion: apps/v1
kind: Deployment
metadata:
  name: spectrum-orchestration
  namespace: production
  labels:
    app: spectrum-orchestration
spec:
  replicas: 1 # By default 1 pod only
  selector:
    matchLabels:
      app: spectrum-orchestration
  template:
    metadata:
      labels:
        app: spectrum-orchestration
    spec:
      containers:
        - name: spectrum-orchestration
          image: <CONTAINER_IMAGE_URL>
          imagePullPolicy: Always
          command: ["sh", "docker-entrypoint.sh"] 
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: "DATABASE_NAME"
              valueFrom:
                secretKeyRef:
                  name: "spectrum-orchestration"
                  key: "DATABASE_NAME"
            - name: "DATABASE_USER"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_USER"
            - name: "DATABASE_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_PASSWORD"
            - name: "DATABASE_HOST"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_HOST"
            - name: "DATABASE_PORT"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_PORT"
            - name: "RABBIT_MQ_URL"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "RABBIT_MQ_URL"
            - name: "CELERY_BACKEND_DATABASE_NAME"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "CELERY_BACKEND_DATABASE_NAME"
            - name: "API_BASE_URL"
              value: "http://spectrum-block-monolith:8000"
            - name: "DJANGO_DEBUG"
              valueFrom:
                secretKeyRef:
                  name: "spectrum-orchestration"
                  key: "DJANGO_DEBUG"
            - name: "SENTRY_DSN"
              valueFrom:
                secretKeyRef:
                  name: "spectrum-orchestration"
                  key: "SENTRY_DSN"
      imagePullSecrets:
        - name: registry-spectrum