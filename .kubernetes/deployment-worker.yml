apiVersion: apps/v1
kind: Deployment
metadata:
  name: spectrum-orchestration-worker
  namespace: production
  labels:
    app: spectrum-orchestration-worker
spec:
  replicas: 1 # By default 1 pod only
  selector:
    matchLabels:
      app: spectrum-orchestration-worker
  template:
    metadata:
      labels:
        app: spectrum-orchestration-worker
    spec:
      containers:
        - name: spectrum-orchestration-worker
          image: <CONTAINER_IMAGE_URL>
          imagePullPolicy: Always
          command: ['celery', '-A', 'orchestration', 'worker', '-l', 'INFO']
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: "DATABASE_NAME"
              valueFrom:
                secretKeyRef:
                  name: "spectrum-orchestration"
                  key: "DATABASE_NAME"
            - name: "DATABASE_USER"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_USER"
            - name: "DATABASE_PASSWORD"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_PASSWORD"
            - name: "DATABASE_HOST"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_HOST"
            - name: "DATABASE_PORT"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "DATABASE_PORT"
            - name: "RABBIT_MQ_URL"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "RABBIT_MQ_URL"
            - name: "CELERY_BACKEND_DATABASE_NAME"
              valueFrom:
                secretKeyRef:
                  name: "spectrum"
                  key: "CELERY_BACKEND_DATABASE_NAME"
            - name: "API_BASE_URL"
              value: "http://spectrum-block-monolith:8000"
      imagePullSecrets:
        - name: registry-spectrum